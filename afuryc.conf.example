# This file is a pattern. You can place it, at $HOME/.afuryc.conf or $XDG_CONFIG_HOME/afuryc.conf ($HOME/.config/afuryc.conf)
# You must apply a chmod 600 on it!
### FTP ###
USERF="foo"
PASSF="foopass"
FTPURL="ftp://afur.archlinux.fr"
### MAKEPKG ###
# PACKAGER is an optional variable. It may be useful when you query package informations, it'll display packager with your name / email address.
# You can live without it!
#PACKAGER="TuxBeastie <foo[AT]unixland.kernel>"
